#include <stdio.h>
bool arama(int n,int a[],int sayi) {
	for (int i = 0; i < n; i++) {
		if (a[i] == sayi) return true;
	}
	return false;
}
int main() {
	int dizi[] = { 1,2,3,4,5,6 };
	int n = (sizeof(dizi) / sizeof(dizi[0]));
	printf("%d", arama(n, dizi, 10));
			
	return 0;
}
